from setuptools import setup, find_packages

setup(
    name='bokeh_and_flask',
    version='0.0.1',
    package=find_packages(where='app'),
    install_requires='',
    description='bokeh_and_flask'
)
